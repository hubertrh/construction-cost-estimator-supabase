export default function AccountMenu() {
  return (
    <div>
      <h1>temp</h1>
      {/* <div>Logged in as: {session?.data?.user?.name}</div> */}
      {/* TODO: Change the following to components */}
      <button>Dashboard</button>
      <button>Sign In</button>
    </div>
  );
}
